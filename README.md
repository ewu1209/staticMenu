# Static Menu

A full-screen static menu for the Empire TV displays.

## Update the menu

Replace `menu.svg` with the final menu image and keep the same filename, or update
the image filename in `index.html`. The display automatically stretches the menu
to fill the entire browser viewport.

GitHub Pages deploys automatically whenever changes are pushed to `main`.
